# System Instructions

## Build Configuration Restrictions
- **DO NOT** edit, modify, or rewrite `.github/workflows/main.yml` unless explicitly requested by the user. Any changes to GitHub Actions workflows or build configurations must be manually approved or applied directly in the GitHub repository by the user.
