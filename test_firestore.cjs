const { initializeApp } = require('firebase/app');
const { initializeFirestore } = require('firebase/firestore');

console.log("initializeFirestore function exists:", typeof initializeFirestore === 'function');
